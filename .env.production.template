# =========================================================
# .env для PRODUCTION (flowoi.ru). НЕ применять пока DNS не готов.
# =========================================================
# Активация:
#   1. Скопировать в /home/ozonpro/app/.env (заменив текущий)
#   2. docker compose up -d --build backend frontend
#   3. docker compose restart nginx
# =========================================================

# --- DATABASE (Selectel managed) ---
# Адрес НЕ меняется. Перед активацией:
# в Selectel панели firewall БД должен пропускать ТОЛЬКО 135.106.158.198/32
DATABASE_URL=postgresql+asyncpg://ozonuser:hczwE5yQ23fk@45.157.160.36:5432/ozonpro?ssl=require
DATABASE_URL_SYNC=postgresql://ozonuser:hczwE5yQ23fk@45.157.160.36:5432/ozonpro?sslmode=require
POSTGRES_USER=ozonuser
POSTGRES_PASSWORD=hczwE5yQ23fk

# --- REDIS (локальный) ---
REDIS_URL=redis://redis:6379/0
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/1

# --- HOSTS / CORS ---
ALLOWED_HOSTS=flowoi.ru,www.flowoi.ru,135.106.158.198
CORS_ORIGINS=https://flowoi.ru,https://www.flowoi.ru
COOKIE_DOMAIN=.flowoi.ru
COOKIE_SECURE=true
COOKIE_SAMESITE=lax

# --- SECRETS ---
# !!! ОБЯЗАТЕЛЬНО сгенерировать НОВЫЕ перед prod:
#   python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_GENERATE_NEW_64_CHAR_TOKEN
FERNET_KEY=CHANGE_ME_GENERATE_NEW_FERNET_KEY
JWT_SECRET=CHANGE_ME_GENERATE_NEW_TOKEN

# --- EMAIL (Mailgun / Sendgrid / SMTP) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@flowoi.ru

# --- LOGGING ---
LOG_LEVEL=INFO
ENVIRONMENT=production

# --- FRONTEND ---
# Vite читает на этапе build, не runtime. После изменения — пересобрать frontend.
VITE_API_URL=https://flowoi.ru/api/v1
