"""
Сезонность — расчётный слой поверх своей истории + категорийный fallback.

Два источника (Принцип 4 — source-флаг):
- A: своя история SKU из order_items (по продажам/выкупам)
- B: категорийный агрегат кабинета через /v1/analytics/data (Premium)

Решение об источнике per-SKU:
- ≥365 дней истории → A («по вашим продажам»)
- 90–364 → A с пометкой «частично», параллельно B если есть
- <90 → fallback на B («по категории в вашем кабинете»)
- если и B пусто → честное «недостаточно данных»

Probe Premium API подтвердил: на Premium Plus нет рыночной нишевой
сезонности РЫНКА Ozon (нужен Premium Pro). /v1/analytics/data с
dimension=["category","month"] отдаёт агрегат СВОИХ SKU по категориям —
это и есть Source B.
"""
